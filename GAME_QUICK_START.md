# 🎮 Быстрый старт игры "Повтори пиксели"

## Как начать играть

### Одиночная игра (Solo)

1. Открой приложение
2. Нажми кнопку **"🎮 Игра"** в переключателе режимов (сверху)
3. Нажми кнопку **"🎯 Одиночная игра"**
4. Игра начнётся автоматически!

**Как играть:**
- Смотри на последовательность зелёных ячеек
- После показа повтори кликами в том же порядке
- С каждым уровнем последовательность становится длиннее
- При ошибке игра заканчивается

### PvP игра (с другом)

1. Открой приложение
2. Нажми **"🎮 Игра"** → **"👥 PvP игра"**
3. Нажми **"Создать игру"**
4. Скопируй код игры (например, `ABC123`)
5. Отправь код другу
6. Друг должен:
   - Открыть приложение
   - Нажать **"🎮 Игра"** → **"👥 PvP игра"**
   - Ввести код и нажать **"Присоединиться"**
7. Когда оба подключены, игра начнётся автоматически

## Уровни сложности

- **Уровни 1-10**: Поле 3x3
- **Уровни 11-20**: Поле 4x4  
- **Уровни 21+**: Поле 5x5

## Просмотр логов

### Все логи backend:
```bash
docker compose logs backend
```

### Только ошибки:
```bash
docker compose logs backend | grep -i "error\|exception\|traceback\|failed"
```

### Логи в реальном времени:
```bash
docker compose logs -f backend
```

### Логи с фильтром по играм:
```bash
docker compose logs backend | grep -i "game\|sequence\|level"
```

### Последние 100 строк:
```bash
docker compose logs --tail=100 backend
```

### Быстрая диагностика игры:
```bash
echo "=== Ошибки ===" && \
docker compose logs --tail=50 backend | grep -i "error\|exception" && \
echo -e "\n=== Игры ===" && \
docker compose logs --tail=30 backend | grep -i "game\|sequence"
```

## Troubleshooting

### Игра не создаётся:
```bash
# Проверить ошибки
docker compose logs backend | grep -i "error\|game"

# Проверить подключение к БД
docker compose exec backend python -c "
from app.core.database import AsyncSessionLocal
from app.models.game import GameSession
import asyncio

async def check():
    async with AsyncSessionLocal() as db:
        from sqlalchemy import select
        result = await db.execute(select(GameSession))
        games = result.scalars().all()
        print(f'Всего игр: {len(games)}')

asyncio.run(check())
"
```

### WebSocket не подключается:
```bash
# Проверить логи WebSocket
docker compose logs backend | grep -i "websocket\|ws/game"
```

### Миграция не применена:
```bash
# Проверить текущую версию
docker compose exec backend alembic current

# Должно показать: 003_add_games (head)

# Если нет - применить
docker compose exec backend alembic upgrade head
```

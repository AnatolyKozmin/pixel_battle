# Руководство по реализации новых функций

## 🎵 Музыка и звуки

### Шаг 1: Добавьте звуковые файлы

1. Скачайте или создайте звуковые файлы (см. `frontend/public/sounds/README.md`)
2. Разместите их в `frontend/public/sounds/`:
   - `background-music.mp3`
   - `pixel-place.mp3`
   - `pixel-error.mp3`
   - `achievement.mp3`
   - `notification.mp3`

### Шаг 2: Проверка работы

Музыка уже интегрирована в `App.vue`:
- Фоновая музыка запускается автоматически
- Звуки воспроизводятся при размещении пикселей
- Кнопка включения/выключения музыки в панели управления

### Рекомендации по звукам:

- **Фоновая музыка**: 30-60 секунд, зацикленная, спокойная мелодия
- **Звук размещения**: Короткий "click" или "pop" (0.1-0.3 сек)
- **Звук ошибки**: Короткий звук ошибки (0.2-0.5 сек)

---

## 🤖 ИИ интеграция

### Вариант 1: OpenAI (рекомендуется)

1. Получите API ключ на [platform.openai.com](https://platform.openai.com)
2. Добавьте в `.env`:
   ```
   OPENAI_API_KEY=sk-your-key-here
   ```
3. Используйте endpoint `/api/ai/analyze` для анализа артов

### Вариант 2: Локальная модель

1. Установите локальный сервер ИИ (например, Ollama)
2. Добавьте в `.env`:
   ```
   USE_LOCAL_AI=true
   LOCAL_AI_URL=http://localhost:11434
   ```
3. Обновите `ai_service.py` для работы с локальной моделью

### Использование:

```javascript
// Анализ участка холста
const response = await fetch('/api/ai/analyze', {
  method: 'POST',
  body: JSON.stringify({
    x_min: 0,
    y_min: 0,
    x_max: 200,
    y_max: 200
  })
})
const analysis = await response.json()
// { description, detected_objects, mood, colors_dominant }
```

---

## 🔄 n8n интеграция

### Шаг 1: Установка n8n

```bash
# Docker
docker run -it --rm --name n8n -p 5678:5678 n8nio/n8n

# Или npm
npm install -g n8n
n8n start
```

### Шаг 2: Настройка webhook URL

В `.env` backend:
```
TELEGRAM_WEBHOOK_URL=https://your-n8n-instance.com/webhook/pixel-battle
```

### Шаг 3: Создание workflow

1. Откройте n8n: `http://localhost:5678`
2. Создайте новый workflow
3. Добавьте Webhook trigger
4. Настройте URL: `https://your-api.com/api/webhooks/n8n/pixel-placed`
5. Добавьте Telegram node для отправки уведомлений

### Примеры workflows:

#### Уведомления о пикселях:
```
Webhook → IF (рядом с пикселем пользователя) → Telegram
```

#### Достижения:
```
Webhook → Switch (100/500/1000 пикселей) → Telegram
```

#### Ежедневная статистика:
```
Cron (00:00) → HTTP Request (статистика) → Template → Telegram
```

---

## 📊 Дополнительные функции

### Автоматическое описание артов

Добавьте периодическую задачу (через Celery или cron):

```python
# В отдельном файле tasks.py
async def analyze_canvas_periodically():
    # Анализируем случайные участки холста
    # Сохраняем описания в БД
    # Показываем игрокам
    pass
```

### Генерация вызовов

Создайте модель Challenge в БД:
- `title` - название вызова
- `description` - описание
- `ai_prompt` - промпт для ИИ проверки
- `reward` - награда

### Достижения и бейджи

Создайте модель Achievement:
- `name` - название
- `description` - описание
- `icon` - иконка
- `condition` - условие получения

---

## 🚀 Быстрый старт

### 1. Музыка (5 минут)
```bash
# Добавьте звуковые файлы в frontend/public/sounds/
# Готово! Музыка уже работает
```

### 2. ИИ анализ (10 минут)
```bash
# Добавьте OPENAI_API_KEY в .env
# Используйте /api/ai/analyze endpoint
```

### 3. n8n уведомления (15 минут)
```bash
# Установите n8n
# Создайте workflow с webhook
# Настройте TELEGRAM_WEBHOOK_URL
```

---

## 💡 Идеи для расширения

1. **Голосовые команды**: "Нарисуй здесь красный квадрат"
2. **AR режим**: Просмотр холста через камеру
3. **Коллаборации**: Несколько игроков рисуют вместе
4. **NFT**: Сохранение артов как NFT
5. **Голосование**: Игроки голосуют за лучшие арты
6. **Темы**: Смена темы холста (космос, природа и т.д.)
7. **Анимация**: Анимированные пиксели
8. **3D режим**: Трехмерный холст

---

## 📝 Чеклист реализации

- [x] Музыка и звуки (базовая реализация)
- [x] ИИ анализ артов (API готов)
- [x] n8n webhooks (API готов)
- [ ] Добавить звуковые файлы
- [ ] Настроить OpenAI API
- [ ] Создать n8n workflows
- [ ] Добавить достижения в БД
- [ ] Создать систему вызовов
- [ ] Добавить автопостинг в соцсети

# 🔄 Жесткий pull с git

## Быстрая команда на сервере

```bash
# Вариант 1: Автоматический скрипт (рекомендуется)
./scripts/hard-pull.sh

# Вариант 2: Вручную (быстро)
git fetch origin
git reset --hard origin/main

# Вариант 3: С очисткой неотслеживаемых файлов
git fetch origin
git reset --hard origin/main
git clean -fd
```

## ⚠️ ВНИМАНИЕ

**Жесткий pull отбросит ВСЕ локальные изменения!**

Если нужно сохранить изменения:
```bash
# Сначала сохраните в stash
git stash push -m "Backup before hard pull"

# Потом делайте жесткий pull
git fetch origin
git reset --hard origin/main

# Если нужно вернуть изменения
git stash pop
```

## Что делает жесткий pull

1. ✅ Получает последние изменения с удаленного репозитория
2. ✅ Отбрасывает все локальные изменения
3. ✅ Синхронизирует репозиторий с удаленной версией
4. ✅ Решает все конфликты (отбрасывая локальные версии)

## После жесткого pull

```bash
# Проверьте статус
git status

# Если нужно настроить для сервера
./scripts/setup-ip.sh YOUR_SERVER_IP
```
